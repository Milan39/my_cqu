import 'package:flutter/material.dart';

class WhatsOnPage extends StatelessWidget {
  const WhatsOnPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Whats on Page"));
  }
}
