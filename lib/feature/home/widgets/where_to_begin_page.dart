import 'package:flutter/material.dart';

class WhereToBeginPage extends StatelessWidget {
  const WhereToBeginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Where to Begin Page"));
  }
}
